{
  "name": "otimizador-shopee-backend",
  "version": "1.0.0",
  "description": "Backend para o Otimizador Inteligente Shopee",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prebuild": "npx puppeteer browsers install chrome",
    "postbuild": "echo \"export const CHROME_PATH='/opt/render/.cache/puppeteer/chrome/linux-127.0.6533.88/chrome-linux64/chrome'\" > chrome-path.js",
    "build": "npx puppeteer browsers list && echo 'Build completed with Chrome installation'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^22.10.0"
  },
  "keywords": [
    "shopee",
    "otimizador",
    "api",
    "puppeteer"
  ],
  "author": "Seu Nome Aqui",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  }
}
